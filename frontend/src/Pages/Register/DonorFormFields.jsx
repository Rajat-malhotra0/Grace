import React from "react";
import "./Register.css";

function DonorFormFields({ formData, handleChange, errors = {} }) {
    return <>{/* Add donor-specific fields here if needed */}</>;
}

export default DonorFormFields;
